services:
  portal:
    build: .
    container_name: awansaya-portal
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      PORT: 3000
      AWANSAYA_API_TOKEN: ${AWANSAYA_API_TOKEN:-}
